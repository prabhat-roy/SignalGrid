name: signalgrid
version: 1.0.0
profile: signalgrid
model-paths: [models]
target-path: target
clean-targets: [target, dbt_packages]

models:
  signalgrid:
    staging:    { +materialized: view }
    grid:       { +materialized: incremental }
    analytics:  { +materialized: table }
