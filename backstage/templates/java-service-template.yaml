apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata: { name: signalgrid_java_service, title: New SignalGrid Java/Spring service, tags: [java, signalgrid] }
spec:
  owner: platform-team
  type: service
  parameters:
    - title: Service info
      required: [name]
      properties: { name: { title: Name, type: string } }
  steps:
    - { id: fetch, name: Fetch, action: fetch:template, input: { url: ./skeletons/java } }
