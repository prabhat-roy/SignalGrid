apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata: { name: signalgrid-dns-error, namespace: chaos-mesh }
spec:
  action: error
  mode: all
  patterns: ["*.signalgrid.svc.cluster.local"]
  selector: { namespaces: [signalgrid] }
  duration: 2m
