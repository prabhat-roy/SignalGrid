apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata: { name: signalgrid-azure, namespace: argocd }
spec:
  generators:
    - git:
        repoURL: https://github.com/prabhat-roy/SignalGrid.git
        revision: HEAD
        directories: [{ path: helm/charts/* }]
  template:
    metadata:
      name: '{{path.basename}}-azure'
      labels: { cloud: azure, signalgrid.io/managed-by: argocd }
    spec:
      project: signalgrid
      source:
        repoURL: https://github.com/prabhat-roy/SignalGrid.git
        targetRevision: HEAD
        path: '{{path}}'
        helm:
          valueFiles: [values.yaml, values-azure.yaml]
      destination:
        server: https://kubernetes.default.svc
        namespace: signalgrid
      syncPolicy:
        automated: { prune: true, selfHeal: true }
        syncOptions: [CreateNamespace=true, ServerSideApply=true]
