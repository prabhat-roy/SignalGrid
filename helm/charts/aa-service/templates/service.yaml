apiVersion: v1
kind: Service
metadata:
  name: {{ include "aa-service.fullname" . }}
  labels: {{- include "aa-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "aa-service.selectorLabels" . | nindent 4 }}
