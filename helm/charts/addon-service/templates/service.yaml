apiVersion: v1
kind: Service
metadata:
  name: {{ include "addon-service.fullname" . }}
  labels: {{- include "addon-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "addon-service.selectorLabels" . | nindent 4 }}
