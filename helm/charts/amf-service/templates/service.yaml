apiVersion: v1
kind: Service
metadata:
  name: {{ include "amf-service.fullname" . }}
  labels: {{- include "amf-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "amf-service.selectorLabels" . | nindent 4 }}
