apiVersion: v1
kind: Service
metadata:
  name: {{ include "b2b-service.fullname" . }}
  labels: {{- include "b2b-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "b2b-service.selectorLabels" . | nindent 4 }}
