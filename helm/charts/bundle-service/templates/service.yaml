apiVersion: v1
kind: Service
metadata:
  name: {{ include "bundle-service.fullname" . }}
  labels: {{- include "bundle-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "bundle-service.selectorLabels" . | nindent 4 }}
