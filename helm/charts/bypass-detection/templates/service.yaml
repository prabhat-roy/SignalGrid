apiVersion: v1
kind: Service
metadata:
  name: {{ include "bypass-detection.fullname" . }}
  labels: {{- include "bypass-detection.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "bypass-detection.selectorLabels" . | nindent 4 }}
