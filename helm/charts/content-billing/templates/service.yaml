apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-billing.fullname" . }}
  labels: {{- include "content-billing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "content-billing.selectorLabels" . | nindent 4 }}
