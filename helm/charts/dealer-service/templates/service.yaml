apiVersion: v1
kind: Service
metadata:
  name: {{ include "dealer-service.fullname" . }}
  labels: {{- include "dealer-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "dealer-service.selectorLabels" . | nindent 4 }}
