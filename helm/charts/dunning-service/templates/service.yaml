apiVersion: v1
kind: Service
metadata:
  name: {{ include "dunning-service.fullname" . }}
  labels: {{- include "dunning-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "dunning-service.selectorLabels" . | nindent 4 }}
