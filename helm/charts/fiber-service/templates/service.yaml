apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiber-service.fullname" . }}
  labels: {{- include "fiber-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fiber-service.selectorLabels" . | nindent 4 }}
