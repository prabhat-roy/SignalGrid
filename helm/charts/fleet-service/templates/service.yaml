apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleet-service.fullname" . }}
  labels: {{- include "fleet-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fleet-service.selectorLabels" . | nindent 4 }}
