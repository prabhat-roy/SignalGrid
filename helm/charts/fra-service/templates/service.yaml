apiVersion: v1
kind: Service
metadata:
  name: {{ include "fra-service.fullname" . }}
  labels: {{- include "fra-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fra-service.selectorLabels" . | nindent 4 }}
