apiVersion: v1
kind: Service
metadata:
  name: {{ include "m2m-service.fullname" . }}
  labels: {{- include "m2m-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "m2m-service.selectorLabels" . | nindent 4 }}
