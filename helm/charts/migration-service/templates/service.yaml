apiVersion: v1
kind: Service
metadata:
  name: {{ include "migration-service.fullname" . }}
  labels: {{- include "migration-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "migration-service.selectorLabels" . | nindent 4 }}
