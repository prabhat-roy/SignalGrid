apiVersion: v1
kind: Service
metadata:
  name: {{ include "ne-service.fullname" . }}
  labels: {{- include "ne-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ne-service.selectorLabels" . | nindent 4 }}
