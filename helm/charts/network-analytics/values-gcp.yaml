cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: signalgrid-network-analytics@{{ .Values.gcp.project }}.iam.gserviceaccount.com
