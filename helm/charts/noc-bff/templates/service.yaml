apiVersion: v1
kind: Service
metadata:
  name: {{ include "noc-bff.fullname" . }}
  labels: {{- include "noc-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "noc-bff.selectorLabels" . | nindent 4 }}
