apiVersion: v1
kind: Service
metadata:
  name: {{ include "number-portability.fullname" . }}
  labels: {{- include "number-portability.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "number-portability.selectorLabels" . | nindent 4 }}
