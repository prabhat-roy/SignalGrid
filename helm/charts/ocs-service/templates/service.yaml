apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocs-service.fullname" . }}
  labels: {{- include "ocs-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ocs-service.selectorLabels" . | nindent 4 }}
