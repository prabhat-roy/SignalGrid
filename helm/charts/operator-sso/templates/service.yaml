apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-sso.fullname" . }}
  labels: {{- include "operator-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "operator-sso.selectorLabels" . | nindent 4 }}
