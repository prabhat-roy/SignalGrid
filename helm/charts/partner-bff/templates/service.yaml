apiVersion: v1
kind: Service
metadata:
  name: {{ include "partner-bff.fullname" . }}
  labels: {{- include "partner-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "partner-bff.selectorLabels" . | nindent 4 }}
