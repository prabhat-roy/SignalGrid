apiVersion: v1
kind: Service
metadata:
  name: {{ include "pm-service.fullname" . }}
  labels: {{- include "pm-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "pm-service.selectorLabels" . | nindent 4 }}
