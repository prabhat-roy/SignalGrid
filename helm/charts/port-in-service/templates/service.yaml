apiVersion: v1
kind: Service
metadata:
  name: {{ include "port-in-service.fullname" . }}
  labels: {{- include "port-in-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "port-in-service.selectorLabels" . | nindent 4 }}
