apiVersion: v1
kind: Service
metadata:
  name: {{ include "rap-service.fullname" . }}
  labels: {{- include "rap-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rap-service.selectorLabels" . | nindent 4 }}
