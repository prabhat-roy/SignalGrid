apiVersion: v1
kind: Service
metadata:
  name: {{ include "rating-engine.fullname" . }}
  labels: {{- include "rating-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rating-engine.selectorLabels" . | nindent 4 }}
