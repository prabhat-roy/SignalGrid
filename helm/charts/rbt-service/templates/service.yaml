apiVersion: v1
kind: Service
metadata:
  name: {{ include "rbt-service.fullname" . }}
  labels: {{- include "rbt-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rbt-service.selectorLabels" . | nindent 4 }}
