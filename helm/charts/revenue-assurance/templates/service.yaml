apiVersion: v1
kind: Service
metadata:
  name: {{ include "revenue-assurance.fullname" . }}
  labels: {{- include "revenue-assurance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "revenue-assurance.selectorLabels" . | nindent 4 }}
