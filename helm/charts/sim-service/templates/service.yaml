apiVersion: v1
kind: Service
metadata:
  name: {{ include "sim-service.fullname" . }}
  labels: {{- include "sim-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "sim-service.selectorLabels" . | nindent 4 }}
