apiVersion: v1
kind: Service
metadata:
  name: {{ include "site-service.fullname" . }}
  labels: {{- include "site-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "site-service.selectorLabels" . | nindent 4 }}
