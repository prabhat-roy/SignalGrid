apiVersion: v1
kind: Service
metadata:
  name: {{ include "smf-service.fullname" . }}
  labels: {{- include "smf-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "smf-service.selectorLabels" . | nindent 4 }}
