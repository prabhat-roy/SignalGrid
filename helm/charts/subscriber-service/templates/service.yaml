apiVersion: v1
kind: Service
metadata:
  name: {{ include "subscriber-service.fullname" . }}
  labels: {{- include "subscriber-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "subscriber-service.selectorLabels" . | nindent 4 }}
