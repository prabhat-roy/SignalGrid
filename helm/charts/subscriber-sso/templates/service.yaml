apiVersion: v1
kind: Service
metadata:
  name: {{ include "subscriber-sso.fullname" . }}
  labels: {{- include "subscriber-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "subscriber-sso.selectorLabels" . | nindent 4 }}
