apiVersion: v1
kind: Service
metadata:
  name: {{ include "support-service.fullname" . }}
  labels: {{- include "support-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "support-service.selectorLabels" . | nindent 4 }}
