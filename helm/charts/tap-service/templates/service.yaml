apiVersion: v1
kind: Service
metadata:
  name: {{ include "tap-service.fullname" . }}
  labels: {{- include "tap-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "tap-service.selectorLabels" . | nindent 4 }}
