apiVersion: v1
kind: Service
metadata:
  name: {{ include "topology-service.fullname" . }}
  labels: {{- include "topology-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "topology-service.selectorLabels" . | nindent 4 }}
