apiVersion: v1
kind: Service
metadata:
  name: {{ include "topup-service.fullname" . }}
  labels: {{- include "topup-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "topup-service.selectorLabels" . | nindent 4 }}
