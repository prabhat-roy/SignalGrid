apiVersion: v1
kind: Service
metadata:
  name: {{ include "usage-service.fullname" . }}
  labels: {{- include "usage-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "usage-service.selectorLabels" . | nindent 4 }}
