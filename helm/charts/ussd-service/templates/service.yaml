apiVersion: v1
kind: Service
metadata:
  name: {{ include "ussd-service.fullname" . }}
  labels: {{- include "ussd-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ussd-service.selectorLabels" . | nindent 4 }}
