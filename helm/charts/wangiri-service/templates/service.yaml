apiVersion: v1
kind: Service
metadata:
  name: {{ include "wangiri-service.fullname" . }}
  labels: {{- include "wangiri-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "wangiri-service.selectorLabels" . | nindent 4 }}
