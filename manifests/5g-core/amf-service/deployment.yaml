apiVersion: apps/v1
kind: Deployment
metadata:
  name: amf-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: amf-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: 5g-core
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: amf-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: amf-service, signalgrid.io/domain: 5g-core }
    spec:
      serviceAccountName: amf-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: amf-service
          image: harbor.signalgrid.internal/signalgrid/amf-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: amf-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
