apiVersion: v1
kind: Service
metadata:
  name: amf-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: amf-service, signalgrid.io/domain: 5g-core }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: amf-service }
