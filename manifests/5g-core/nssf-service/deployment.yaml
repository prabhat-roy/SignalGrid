apiVersion: apps/v1
kind: Deployment
metadata:
  name: nssf-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: nssf-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: 5g-core
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: nssf-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: nssf-service, signalgrid.io/domain: 5g-core }
    spec:
      serviceAccountName: nssf-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: nssf-service
          image: harbor.signalgrid.internal/signalgrid/nssf-service:latest
          ports: [{ containerPort: 50164, name: http }]
          envFrom: [{ configMapRef: { name: nssf-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50164 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50164 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
