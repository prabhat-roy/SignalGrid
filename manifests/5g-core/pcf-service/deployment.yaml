apiVersion: apps/v1
kind: Deployment
metadata:
  name: pcf-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: pcf-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: 5g-core
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pcf-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pcf-service, signalgrid.io/domain: 5g-core }
    spec:
      serviceAccountName: pcf-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pcf-service
          image: harbor.signalgrid.internal/signalgrid/pcf-service:latest
          ports: [{ containerPort: 50163, name: http }]
          envFrom: [{ configMapRef: { name: pcf-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50163 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50163 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
