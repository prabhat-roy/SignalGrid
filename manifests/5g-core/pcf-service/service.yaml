apiVersion: v1
kind: Service
metadata:
  name: pcf-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: pcf-service, signalgrid.io/domain: 5g-core }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: pcf-service }
