apiVersion: apps/v1
kind: Deployment
metadata:
  name: smf-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: smf-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: 5g-core
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: smf-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: smf-service, signalgrid.io/domain: 5g-core }
    spec:
      serviceAccountName: smf-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: smf-service
          image: harbor.signalgrid.internal/signalgrid/smf-service:latest
          ports: [{ containerPort: 50161, name: http }]
          envFrom: [{ configMapRef: { name: smf-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
