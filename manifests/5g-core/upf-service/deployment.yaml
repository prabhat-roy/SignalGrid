apiVersion: apps/v1
kind: Deployment
metadata:
  name: upf-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: upf-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: 5g-core
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: upf-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: upf-service, signalgrid.io/domain: 5g-core }
    spec:
      serviceAccountName: upf-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: upf-service
          image: harbor.signalgrid.internal/signalgrid/upf-service:latest
          ports: [{ containerPort: 50162, name: http }]
          envFrom: [{ configMapRef: { name: upf-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50162 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50162 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
