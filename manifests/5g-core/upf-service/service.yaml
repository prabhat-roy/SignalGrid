apiVersion: v1
kind: Service
metadata:
  name: upf-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: upf-service, signalgrid.io/domain: 5g-core }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: upf-service }
