apiVersion: apps/v1
kind: Deployment
metadata:
  name: capacity-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: capacity-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: capacity-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: capacity-service, signalgrid.io/domain: analytics }
    spec:
      serviceAccountName: capacity-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: capacity-service
          image: harbor.signalgrid.internal/signalgrid/capacity-service:latest
          ports: [{ containerPort: 50303, name: http }]
          envFrom: [{ configMapRef: { name: capacity-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50303 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50303 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
