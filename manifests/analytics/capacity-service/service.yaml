apiVersion: v1
kind: Service
metadata:
  name: capacity-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: capacity-service, signalgrid.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50303, targetPort: 50303 }]
  selector: { app.kubernetes.io/name: capacity-service }
