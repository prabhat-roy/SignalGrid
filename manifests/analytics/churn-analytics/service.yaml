apiVersion: v1
kind: Service
metadata:
  name: churn-analytics
  namespace: signalgrid
  labels: { app.kubernetes.io/name: churn-analytics, signalgrid.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50302, targetPort: 50302 }]
  selector: { app.kubernetes.io/name: churn-analytics }
