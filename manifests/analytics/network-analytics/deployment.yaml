apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-analytics
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: network-analytics
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: network-analytics } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: network-analytics, signalgrid.io/domain: analytics }
    spec:
      serviceAccountName: network-analytics
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: network-analytics
          image: harbor.signalgrid.internal/signalgrid/network-analytics:latest
          ports: [{ containerPort: 50300, name: http }]
          envFrom: [{ configMapRef: { name: network-analytics } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50300 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50300 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
