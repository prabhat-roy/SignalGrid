apiVersion: v1
kind: Service
metadata:
  name: network-analytics
  namespace: signalgrid
  labels: { app.kubernetes.io/name: network-analytics, signalgrid.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: network-analytics }
