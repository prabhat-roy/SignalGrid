apiVersion: v1
kind: Service
metadata:
  name: revenue-analytics
  namespace: signalgrid
  labels: { app.kubernetes.io/name: revenue-analytics, signalgrid.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: revenue-analytics }
