apiVersion: apps/v1
kind: Deployment
metadata:
  name: dunning-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: dunning-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: billing
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: dunning-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: dunning-service, signalgrid.io/domain: billing }
    spec:
      serviceAccountName: dunning-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: dunning-service
          image: harbor.signalgrid.internal/signalgrid/dunning-service:latest
          ports: [{ containerPort: 50103, name: http }]
          envFrom: [{ configMapRef: { name: dunning-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
