apiVersion: v1
kind: Service
metadata:
  name: dunning-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: dunning-service, signalgrid.io/domain: billing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50103, targetPort: 50103 }]
  selector: { app.kubernetes.io/name: dunning-service }
