apiVersion: apps/v1
kind: Deployment
metadata:
  name: invoice-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: invoice-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: billing
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: invoice-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: invoice-service, signalgrid.io/domain: billing }
    spec:
      serviceAccountName: invoice-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: invoice-service
          image: harbor.signalgrid.internal/signalgrid/invoice-service:latest
          ports: [{ containerPort: 50102, name: http }]
          envFrom: [{ configMapRef: { name: invoice-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
