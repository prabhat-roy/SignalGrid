apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: invoice-service, signalgrid.io/domain: billing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: invoice-service }
