apiVersion: apps/v1
kind: Deployment
metadata:
  name: mediation-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: mediation-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: billing
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: mediation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: mediation-service, signalgrid.io/domain: billing }
    spec:
      serviceAccountName: mediation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: mediation-service
          image: harbor.signalgrid.internal/signalgrid/mediation-service:latest
          ports: [{ containerPort: 50100, name: http }]
          envFrom: [{ configMapRef: { name: mediation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50100 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50100 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
