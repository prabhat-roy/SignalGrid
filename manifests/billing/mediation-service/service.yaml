apiVersion: v1
kind: Service
metadata:
  name: mediation-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: mediation-service, signalgrid.io/domain: billing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50100, targetPort: 50100 }]
  selector: { app.kubernetes.io/name: mediation-service }
