apiVersion: v1
kind: Service
metadata:
  name: rating-engine
  namespace: signalgrid
  labels: { app.kubernetes.io/name: rating-engine, signalgrid.io/domain: billing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50101, targetPort: 50101 }]
  selector: { app.kubernetes.io/name: rating-engine }
