apiVersion: apps/v1
kind: Deployment
metadata:
  name: b2b-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: b2b-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: channel
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: b2b-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: b2b-service, signalgrid.io/domain: channel }
    spec:
      serviceAccountName: b2b-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: b2b-service
          image: harbor.signalgrid.internal/signalgrid/b2b-service:latest
          ports: [{ containerPort: 50244, name: http }]
          envFrom: [{ configMapRef: { name: b2b-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50244 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50244 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
