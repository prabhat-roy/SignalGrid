apiVersion: v1
kind: Service
metadata:
  name: b2b-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: b2b-service, signalgrid.io/domain: channel }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50244, targetPort: 50244 }]
  selector: { app.kubernetes.io/name: b2b-service }
