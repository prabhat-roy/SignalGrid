apiVersion: v1
kind: Service
metadata:
  name: commission-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: commission-service, signalgrid.io/domain: channel }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: commission-service }
