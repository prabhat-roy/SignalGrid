apiVersion: apps/v1
kind: Deployment
metadata:
  name: dealer-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: dealer-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: channel
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: dealer-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: dealer-service, signalgrid.io/domain: channel }
    spec:
      serviceAccountName: dealer-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: dealer-service
          image: harbor.signalgrid.internal/signalgrid/dealer-service:latest
          ports: [{ containerPort: 50240, name: http }]
          envFrom: [{ configMapRef: { name: dealer-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
