apiVersion: v1
kind: Service
metadata:
  name: dealer-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: dealer-service, signalgrid.io/domain: channel }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: dealer-service }
