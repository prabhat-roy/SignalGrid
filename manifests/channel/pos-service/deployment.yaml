apiVersion: apps/v1
kind: Deployment
metadata:
  name: pos-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: pos-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: channel
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pos-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pos-service, signalgrid.io/domain: channel }
    spec:
      serviceAccountName: pos-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pos-service
          image: harbor.signalgrid.internal/signalgrid/pos-service:latest
          ports: [{ containerPort: 50241, name: http }]
          envFrom: [{ configMapRef: { name: pos-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50241 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50241 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
