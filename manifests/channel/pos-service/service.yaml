apiVersion: v1
kind: Service
metadata:
  name: pos-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: pos-service, signalgrid.io/domain: channel }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50241, targetPort: 50241 }]
  selector: { app.kubernetes.io/name: pos-service }
