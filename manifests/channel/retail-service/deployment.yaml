apiVersion: apps/v1
kind: Deployment
metadata:
  name: retail-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: retail-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: channel
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: retail-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: retail-service, signalgrid.io/domain: channel }
    spec:
      serviceAccountName: retail-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: retail-service
          image: harbor.signalgrid.internal/signalgrid/retail-service:latest
          ports: [{ containerPort: 50243, name: http }]
          envFrom: [{ configMapRef: { name: retail-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
