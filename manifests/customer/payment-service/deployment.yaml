apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: payment-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: payment-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: payment-service, signalgrid.io/domain: customer }
    spec:
      serviceAccountName: payment-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: payment-service
          image: harbor.signalgrid.internal/signalgrid/payment-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: payment-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
