apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: support-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: support-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: support-service, signalgrid.io/domain: customer }
    spec:
      serviceAccountName: support-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: support-service
          image: harbor.signalgrid.internal/signalgrid/support-service:latest
          ports: [{ containerPort: 50221, name: http }]
          envFrom: [{ configMapRef: { name: support-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
