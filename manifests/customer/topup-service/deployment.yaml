apiVersion: apps/v1
kind: Deployment
metadata:
  name: topup-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: topup-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: topup-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: topup-service, signalgrid.io/domain: customer }
    spec:
      serviceAccountName: topup-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: topup-service
          image: harbor.signalgrid.internal/signalgrid/topup-service:latest
          ports: [{ containerPort: 50223, name: http }]
          envFrom: [{ configMapRef: { name: topup-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50223 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50223 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
