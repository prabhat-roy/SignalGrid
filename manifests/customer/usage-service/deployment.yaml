apiVersion: apps/v1
kind: Deployment
metadata:
  name: usage-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: usage-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: usage-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: usage-service, signalgrid.io/domain: customer }
    spec:
      serviceAccountName: usage-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: usage-service
          image: harbor.signalgrid.internal/signalgrid/usage-service:latest
          ports: [{ containerPort: 50222, name: http }]
          envFrom: [{ configMapRef: { name: usage-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
