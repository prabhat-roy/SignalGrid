apiVersion: v1
kind: Service
metadata:
  name: usage-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: usage-service, signalgrid.io/domain: customer }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: usage-service }
