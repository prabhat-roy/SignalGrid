apiVersion: v1
kind: Service
metadata:
  name: bypass-detection
  namespace: signalgrid
  labels: { app.kubernetes.io/name: bypass-detection, signalgrid.io/domain: fraud }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: bypass-detection }
