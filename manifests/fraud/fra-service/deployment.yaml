apiVersion: apps/v1
kind: Deployment
metadata:
  name: fra-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: fra-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: fraud
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fra-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fra-service, signalgrid.io/domain: fraud }
    spec:
      serviceAccountName: fra-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fra-service
          image: harbor.signalgrid.internal/signalgrid/fra-service:latest
          ports: [{ containerPort: 50260, name: http }]
          envFrom: [{ configMapRef: { name: fra-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
