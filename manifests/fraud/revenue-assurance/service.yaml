apiVersion: v1
kind: Service
metadata:
  name: revenue-assurance
  namespace: signalgrid
  labels: { app.kubernetes.io/name: revenue-assurance, signalgrid.io/domain: fraud }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: revenue-assurance }
