apiVersion: v1
kind: Service
metadata:
  name: sim-swap
  namespace: signalgrid
  labels: { app.kubernetes.io/name: sim-swap, signalgrid.io/domain: fraud }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50263, targetPort: 50263 }]
  selector: { app.kubernetes.io/name: sim-swap }
