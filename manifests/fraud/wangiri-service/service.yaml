apiVersion: v1
kind: Service
metadata:
  name: wangiri-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: wangiri-service, signalgrid.io/domain: fraud }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50264, targetPort: 50264 }]
  selector: { app.kubernetes.io/name: wangiri-service }
