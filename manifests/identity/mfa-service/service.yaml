apiVersion: v1
kind: Service
metadata:
  name: mfa-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: mfa-service, signalgrid.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50023, targetPort: 50023 }]
  selector: { app.kubernetes.io/name: mfa-service }
