apiVersion: v1
kind: Service
metadata:
  name: fiber-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: fiber-service, signalgrid.io/domain: inventory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50123, targetPort: 50123 }]
  selector: { app.kubernetes.io/name: fiber-service }
