apiVersion: v1
kind: Service
metadata:
  name: ne-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: ne-service, signalgrid.io/domain: inventory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: ne-service }
