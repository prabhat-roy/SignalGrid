apiVersion: apps/v1
kind: Deployment
metadata:
  name: site-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: site-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: inventory
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: site-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: site-service, signalgrid.io/domain: inventory }
    spec:
      serviceAccountName: site-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: site-service
          image: harbor.signalgrid.internal/signalgrid/site-service:latest
          ports: [{ containerPort: 50121, name: http }]
          envFrom: [{ configMapRef: { name: site-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
