apiVersion: v1
kind: Service
metadata:
  name: site-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: site-service, signalgrid.io/domain: inventory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: site-service }
