apiVersion: apps/v1
kind: Deployment
metadata:
  name: topology-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: topology-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: inventory
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: topology-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: topology-service, signalgrid.io/domain: inventory }
    spec:
      serviceAccountName: topology-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: topology-service
          image: harbor.signalgrid.internal/signalgrid/topology-service:latest
          ports: [{ containerPort: 50120, name: http }]
          envFrom: [{ configMapRef: { name: topology-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
