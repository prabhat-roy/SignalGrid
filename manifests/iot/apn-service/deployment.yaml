apiVersion: apps/v1
kind: Deployment
metadata:
  name: apn-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: apn-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: iot
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: apn-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: apn-service, signalgrid.io/domain: iot }
    spec:
      serviceAccountName: apn-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: apn-service
          image: harbor.signalgrid.internal/signalgrid/apn-service:latest
          ports: [{ containerPort: 50201, name: http }]
          envFrom: [{ configMapRef: { name: apn-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
