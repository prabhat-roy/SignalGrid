apiVersion: v1
kind: Service
metadata:
  name: apn-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: apn-service, signalgrid.io/domain: iot }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: apn-service }
