apiVersion: v1
kind: Service
metadata:
  name: device-management
  namespace: signalgrid
  labels: { app.kubernetes.io/name: device-management, signalgrid.io/domain: iot }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: device-management }
