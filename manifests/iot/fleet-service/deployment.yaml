apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleet-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: fleet-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: iot
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fleet-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fleet-service, signalgrid.io/domain: iot }
    spec:
      serviceAccountName: fleet-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fleet-service
          image: harbor.signalgrid.internal/signalgrid/fleet-service:latest
          ports: [{ containerPort: 50203, name: http }]
          envFrom: [{ configMapRef: { name: fleet-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50203 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50203 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
