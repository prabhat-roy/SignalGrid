apiVersion: v1
kind: Service
metadata:
  name: fleet-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: fleet-service, signalgrid.io/domain: iot }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50203, targetPort: 50203 }]
  selector: { app.kubernetes.io/name: fleet-service }
