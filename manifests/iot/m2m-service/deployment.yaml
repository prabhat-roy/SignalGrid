apiVersion: apps/v1
kind: Deployment
metadata:
  name: m2m-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: m2m-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: iot
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: m2m-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: m2m-service, signalgrid.io/domain: iot }
    spec:
      serviceAccountName: m2m-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: m2m-service
          image: harbor.signalgrid.internal/signalgrid/m2m-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: m2m-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
