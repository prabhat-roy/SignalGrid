apiVersion: v1
kind: Service
metadata:
  name: m2m-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: m2m-service, signalgrid.io/domain: iot }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: m2m-service }
