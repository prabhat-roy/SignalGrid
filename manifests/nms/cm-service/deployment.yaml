apiVersion: apps/v1
kind: Deployment
metadata:
  name: cm-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: cm-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: nms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: cm-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: cm-service, signalgrid.io/domain: nms }
    spec:
      serviceAccountName: cm-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: cm-service
          image: harbor.signalgrid.internal/signalgrid/cm-service:latest
          ports: [{ containerPort: 50142, name: http }]
          envFrom: [{ configMapRef: { name: cm-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
