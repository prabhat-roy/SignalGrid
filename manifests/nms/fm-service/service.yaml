apiVersion: v1
kind: Service
metadata:
  name: fm-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: fm-service, signalgrid.io/domain: nms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50140, targetPort: 50140 }]
  selector: { app.kubernetes.io/name: fm-service }
