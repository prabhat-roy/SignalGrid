apiVersion: apps/v1
kind: Deployment
metadata:
  name: pm-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: pm-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: nms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pm-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pm-service, signalgrid.io/domain: nms }
    spec:
      serviceAccountName: pm-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pm-service
          image: harbor.signalgrid.internal/signalgrid/pm-service:latest
          ports: [{ containerPort: 50141, name: http }]
          envFrom: [{ configMapRef: { name: pm-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
