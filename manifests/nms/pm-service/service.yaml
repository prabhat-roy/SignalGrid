apiVersion: v1
kind: Service
metadata:
  name: pm-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: pm-service, signalgrid.io/domain: nms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: pm-service }
