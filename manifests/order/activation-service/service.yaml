apiVersion: v1
kind: Service
metadata:
  name: activation-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: activation-service, signalgrid.io/domain: order }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50081, targetPort: 50081 }]
  selector: { app.kubernetes.io/name: activation-service }
