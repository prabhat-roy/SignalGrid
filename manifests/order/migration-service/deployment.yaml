apiVersion: apps/v1
kind: Deployment
metadata:
  name: migration-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: migration-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: order
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: migration-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: migration-service, signalgrid.io/domain: order }
    spec:
      serviceAccountName: migration-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: migration-service
          image: harbor.signalgrid.internal/signalgrid/migration-service:latest
          ports: [{ containerPort: 50083, name: http }]
          envFrom: [{ configMapRef: { name: migration-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50083 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50083 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
