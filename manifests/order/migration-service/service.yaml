apiVersion: v1
kind: Service
metadata:
  name: migration-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: migration-service, signalgrid.io/domain: order }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50083, targetPort: 50083 }]
  selector: { app.kubernetes.io/name: migration-service }
