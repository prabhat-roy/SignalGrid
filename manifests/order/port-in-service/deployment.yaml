apiVersion: apps/v1
kind: Deployment
metadata:
  name: port-in-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: port-in-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: order
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: port-in-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: port-in-service, signalgrid.io/domain: order }
    spec:
      serviceAccountName: port-in-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: port-in-service
          image: harbor.signalgrid.internal/signalgrid/port-in-service:latest
          ports: [{ containerPort: 50082, name: http }]
          envFrom: [{ configMapRef: { name: port-in-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
