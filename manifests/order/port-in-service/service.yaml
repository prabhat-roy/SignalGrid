apiVersion: v1
kind: Service
metadata:
  name: port-in-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: port-in-service, signalgrid.io/domain: order }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50082, targetPort: 50082 }]
  selector: { app.kubernetes.io/name: port-in-service }
