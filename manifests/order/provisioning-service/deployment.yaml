apiVersion: apps/v1
kind: Deployment
metadata:
  name: provisioning-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: provisioning-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: order
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: provisioning-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: provisioning-service, signalgrid.io/domain: order }
    spec:
      serviceAccountName: provisioning-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: provisioning-service
          image: harbor.signalgrid.internal/signalgrid/provisioning-service:latest
          ports: [{ containerPort: 50080, name: http }]
          envFrom: [{ configMapRef: { name: provisioning-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50080 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50080 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
