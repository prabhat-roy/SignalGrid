apiVersion: v1
kind: Service
metadata:
  name: audit-log-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: audit-log-service, signalgrid.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51008, targetPort: 51008 }]
  selector: { app.kubernetes.io/name: audit-log-service }
