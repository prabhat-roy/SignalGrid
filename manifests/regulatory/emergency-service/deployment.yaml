apiVersion: apps/v1
kind: Deployment
metadata:
  name: emergency-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: emergency-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: regulatory
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: emergency-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: emergency-service, signalgrid.io/domain: regulatory }
    spec:
      serviceAccountName: emergency-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: emergency-service
          image: harbor.signalgrid.internal/signalgrid/emergency-service:latest
          ports: [{ containerPort: 50282, name: http }]
          envFrom: [{ configMapRef: { name: emergency-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50282 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50282 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
