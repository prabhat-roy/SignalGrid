apiVersion: v1
kind: Service
metadata:
  name: lawful-intercept
  namespace: signalgrid
  labels: { app.kubernetes.io/name: lawful-intercept, signalgrid.io/domain: regulatory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: lawful-intercept }
