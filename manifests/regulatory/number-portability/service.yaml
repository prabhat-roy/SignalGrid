apiVersion: v1
kind: Service
metadata:
  name: number-portability
  namespace: signalgrid
  labels: { app.kubernetes.io/name: number-portability, signalgrid.io/domain: regulatory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: number-portability }
