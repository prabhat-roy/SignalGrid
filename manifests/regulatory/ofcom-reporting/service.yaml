apiVersion: v1
kind: Service
metadata:
  name: ofcom-reporting
  namespace: signalgrid
  labels: { app.kubernetes.io/name: ofcom-reporting, signalgrid.io/domain: regulatory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50283, targetPort: 50283 }]
  selector: { app.kubernetes.io/name: ofcom-reporting }
