apiVersion: apps/v1
kind: Deployment
metadata:
  name: aa-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: aa-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: roaming
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: aa-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: aa-service, signalgrid.io/domain: roaming }
    spec:
      serviceAccountName: aa-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: aa-service
          image: harbor.signalgrid.internal/signalgrid/aa-service:latest
          ports: [{ containerPort: 50183, name: http }]
          envFrom: [{ configMapRef: { name: aa-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50183 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50183 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
