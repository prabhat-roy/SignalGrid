apiVersion: v1
kind: Service
metadata:
  name: aa-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: aa-service, signalgrid.io/domain: roaming }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50183, targetPort: 50183 }]
  selector: { app.kubernetes.io/name: aa-service }
