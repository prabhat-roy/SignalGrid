apiVersion: v1
kind: Service
metadata:
  name: ocs-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: ocs-service, signalgrid.io/domain: roaming }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: ocs-service }
