apiVersion: apps/v1
kind: Deployment
metadata:
  name: rap-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: rap-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: roaming
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: rap-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: rap-service, signalgrid.io/domain: roaming }
    spec:
      serviceAccountName: rap-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: rap-service
          image: harbor.signalgrid.internal/signalgrid/rap-service:latest
          ports: [{ containerPort: 50181, name: http }]
          envFrom: [{ configMapRef: { name: rap-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
