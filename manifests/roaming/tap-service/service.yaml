apiVersion: v1
kind: Service
metadata:
  name: tap-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: tap-service, signalgrid.io/domain: roaming }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: tap-service }
