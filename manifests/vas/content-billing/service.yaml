apiVersion: v1
kind: Service
metadata:
  name: content-billing
  namespace: signalgrid
  labels: { app.kubernetes.io/name: content-billing, signalgrid.io/domain: vas }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50323, targetPort: 50323 }]
  selector: { app.kubernetes.io/name: content-billing }
