apiVersion: apps/v1
kind: Deployment
metadata:
  name: rbt-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: rbt-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: vas
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: rbt-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: rbt-service, signalgrid.io/domain: vas }
    spec:
      serviceAccountName: rbt-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: rbt-service
          image: harbor.signalgrid.internal/signalgrid/rbt-service:latest
          ports: [{ containerPort: 50322, name: http }]
          envFrom: [{ configMapRef: { name: rbt-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50322 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50322 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
