apiVersion: v1
kind: Service
metadata:
  name: rbt-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: rbt-service, signalgrid.io/domain: vas }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50322, targetPort: 50322 }]
  selector: { app.kubernetes.io/name: rbt-service }
