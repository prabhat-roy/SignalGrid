apiVersion: apps/v1
kind: Deployment
metadata:
  name: ussd-service
  namespace: signalgrid
  labels:
    app.kubernetes.io/name: ussd-service
    app.kubernetes.io/part-of: signalgrid
    signalgrid.io/domain: vas
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ussd-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ussd-service, signalgrid.io/domain: vas }
    spec:
      serviceAccountName: ussd-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ussd-service
          image: harbor.signalgrid.internal/signalgrid/ussd-service:latest
          ports: [{ containerPort: 50321, name: http }]
          envFrom: [{ configMapRef: { name: ussd-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50321 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50321 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
