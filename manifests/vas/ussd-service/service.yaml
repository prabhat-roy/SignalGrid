apiVersion: v1
kind: Service
metadata:
  name: ussd-service
  namespace: signalgrid
  labels: { app.kubernetes.io/name: ussd-service, signalgrid.io/domain: vas }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50321, targetPort: 50321 }]
  selector: { app.kubernetes.io/name: ussd-service }
