global:
  novuVersion: 1.0.0
api: { ingress: { enabled: true, hosts: [{ host: novu-api.signalgrid.internal }] } }
web: { ingress: { enabled: true, hosts: [{ host: novu.signalgrid.internal }] } }
mongodb: { enabled: true }
redis:   { enabled: true }
