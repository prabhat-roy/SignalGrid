opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: signalgrid-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.signalgrid.internal, paths: [{ path: /, pathType: Prefix }] }]
