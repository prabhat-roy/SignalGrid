apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata: { name: signalgrid-vault-issuer }
spec:
  vault:
    server: https://vault.signalgrid.internal
    path: pki/sign/signalgrid-mesh
    auth:
      kubernetes:
        role: cert-manager
        mountPath: /v1/auth/kubernetes
        secretRef: { name: cert-manager-vault-token, key: token }
