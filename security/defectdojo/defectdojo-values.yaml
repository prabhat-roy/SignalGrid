tag: 2.36.0
host: defectdojo.signalgrid.internal
extraEnv:
  - { name: DD_DATABASE_URL, valueFrom: { secretKeyRef: { name: defectdojo-postgres, key: url } } }
redis: { enabled: true }
celery:
  worker: { replicaCount: 2 }
ingress:
  enabled: true
  hosts: [{ host: defectdojo.signalgrid.internal, paths: [{ path: /, pathType: Prefix }] }]
