# Skaffold pipeline for SignalGrid
apiVersion: skaffold/v4beta11
kind: Config
metadata: { name: signalgrid }
build:
  artifacts:
    - { image: signalgrid/api-gateway, context: src/api-gateway }
    - { image: signalgrid/identity-service, context: src/identity-service }
  local: { push: false }
deploy:
  helm:
    releases:
      - { name: api-gateway, chartPath: helm/charts/api-gateway, namespace: signalgrid }
profiles:
  - name: prod
    deploy: { kubectl: { manifests: ["manifests/prod/*.yaml"] } }
