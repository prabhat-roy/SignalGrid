apiVersion: temporal.io/v1beta1
kind: TemporalNamespace
metadata: { name: signalgrid, namespace: temporal-system }
spec:
  retentionPeriod: 30d
  archival:
    history:    { state: enabled, uri: s3://signalgrid-temporal-archive/history }
    visibility: { state: enabled, uri: s3://signalgrid-temporal-archive/visibility }
